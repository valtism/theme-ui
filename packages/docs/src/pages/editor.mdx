import Layout from '../components/layout'

export default Layout
export const title = '@theme-ui/editor'

import { Editor, ColorPalette } from '@theme-ui/editor'

# @theme-ui/editor

Components for live-editing apps built with Theme UI.

<Editor>
  <ColorPalette
    size={96}
    omit={[ 'modes' ]}
  />
</Editor>

## To do

- [x] Base app/state component
- [ ] Colors
- [ ] Typography
